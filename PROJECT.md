# Project

## Project
- Repository: bnewm0609/learning-tools-sepo — personal learning tools (Python). Core stack: items.md (Q/A input), parse_items.py (JSON parser), quiz.py (interactive quiz), export_anki.py (Anki TSV export). Anki is the SRS backend; local tools handle capture and browsing only.
- Auth: OIDC broker. Model provider: claude (CLAUDE_CODE_OAUTH_TOKEN). Agent infra: v0.3.0 upgrade pending (PR #8); new optional secret AGENT_MODEL_POLICY enables per-route model/reasoning-effort selection. Rubrics branch initialized.

## Open Questions
